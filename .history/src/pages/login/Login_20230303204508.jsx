import React from "react";
import PropTypes from "prop-types";
import { FormHook } from "@omponents";

const Login = (props) => {
  return (
    <div>
      <FormHook />
    </div>
  );
};

Login.propTypes = {};

export default Login;
