import React from "react";
import PropTypes from "prop-types";

const State = ({ text, code }) => {
  return <div className="px-2 py-1 rounded-md">{text}</div>;
};

State.propTypes = {};

export default State;
