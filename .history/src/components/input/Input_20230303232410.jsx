import React from "react";
import PropTypes from "prop-types";

const Input = (props) => {
  return (
    <input
      type="text"
      className="w-full outline-none border border-light-gray-border rounded-md"
    />
  );
};

Input.propTypes = {};

export default Input;
