import React from "react";
import PropTypes from "prop-types";

const DefaultLayout = (props) => {
  return <div>default lay out ne</div>;
};

DefaultLayout.propTypes = {};

export default DefaultLayout;
