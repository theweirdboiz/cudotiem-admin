import React from "react";
import PropTypes from "prop-types";

const Header = (props) => {
  return <header className="w-full fixed bg-primary-color">header</header>;
};

Header.propTypes = {};

export default Header;
